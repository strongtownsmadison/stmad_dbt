
version: 2

models:
  - name: parcel_x_ref_bridge
    description: "This model selects distinct combinations of parcel_id, x_ref_parcel, and parcel_year from the referenced parcels_column_rename model."
    columns:
      - name: parcel_id
        data_type: character varying
        description: '{{ doc("parcel_id") }}'

      - name: x_ref_parcel
        data_type: character varying
        description: '{{ doc("x_ref_parcel") }}'

      - name: parcel_year
        data_type: double precision
        description: '{{ doc("parcel_year") }}'
